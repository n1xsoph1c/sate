{
    "compilerOptions": {
      "target": "es2015",            // Specify your target ECMAScript version.
      "module": "commonjs",          // Specify the module system (e.g., commonjs, esnext).
      "outDir": "./dist",            // Specify the output directory for compiled files.
      "jsx": "react",
      "esModuleInterop":true,
    },
    "include": ["core/**/*.ts", "index.ts"],      // Specify files to include in compilation.
    "exclude": ["node_modules"]      // Specify files or directories to exclude from compilation.
  }
  